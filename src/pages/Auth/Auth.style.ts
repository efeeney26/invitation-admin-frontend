import styled from '@emotion/styled';

import { ContainerStyled } from '../Main/Main.style';

export const AuthContainerStyled = styled(ContainerStyled)({
  display: 'flex',
  flexDirection: 'column',
  alignItems: 'center',
  justifyContent: 'center',
  padding: '15% 30%',
});
